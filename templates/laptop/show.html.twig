{% extends 'base.html.twig' %}

{% block title %}Laptop{% endblock %}

{% block body %}
    <h1>Laptop</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ laptop.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ laptop.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ laptop.price }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ laptop.quantity }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ laptop.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_laptop_index') }}">back to list</a>

    <a href="{{ path('app_laptop_edit', {'id': laptop.id}) }}">edit</a>

    {{ include('laptop/_delete_form.html.twig') }}
{% endblock %}
